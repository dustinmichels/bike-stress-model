services:
  - type: web
    name: fastapi-backend
    rootDir: backend
    runtime: python
    buildCommand: "uv sync"
    startCommand: "uv run uvicorn api:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
